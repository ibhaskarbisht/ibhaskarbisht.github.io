# ibhaskarbisht.github.io
It is a site which takes an image having some text (Not-Handwritten) and it find complex words from the image.We have already a list of some complex words in our database which will match to the image.
